# Backend boundary

No API server is implemented in v0.1. Proposed endpoints: authenticated projects, versioned content revisions, generation jobs and authoritative encounters. Read `../../docs/ARCHITECTURE.md`. Server-owned state and provider credentials must never be moved into the browser build.
